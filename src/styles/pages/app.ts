import { styled } from "..";

export const Container =  styled('div', {
  minHeight: '100vh',
})

export const Header = styled('header', {
  padding: '2rem 0',
  widht: '100%',
  maxWidth: 1180,
  margin: ' 0 auto',
})